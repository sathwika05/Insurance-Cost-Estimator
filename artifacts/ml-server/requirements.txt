# Pinned: the serialized model in models/insurance_model.joblib was created with
# these exact scikit-learn / numpy / scipy versions. Unpickling under different
# versions can warn or fail outright, so bump these only after re-exporting the model.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
scikit-learn==1.9.0
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
joblib==1.5.3
